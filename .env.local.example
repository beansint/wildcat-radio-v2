PORT=3011
NEXT_PUBLIC_API_URL=http://localhost:3010
NEXT_PUBLIC_STATION_UTC_OFFSET_MINUTES=480
NEXT_PUBLIC_MEDIA_HOST=pub-3d516b7425bc416288580567af2cb662.r2.dev
# Absolute origin used for metadataBase, robots.txt and sitemap.xml. There is
# no production domain yet (the university subdomain is still pending) — set
# this to the real origin once one exists. Defaults to the dev server origin.
NEXT_PUBLIC_SITE_URL=http://localhost:3011
